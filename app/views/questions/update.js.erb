$('.question-errors').html('<%= render 'shared/errors', resource: @question %>');

<% unless @question.errors.present? %>
  $('.question p.title').html('<%= @question.title %>');
  $('.question p.body').html('<%= @question.body %>');
  $('.question p.files').html('<%= render 'shared/files', resource: @question %>');


  $('form.question-edit').addClass('hidden');
  $('.edit-question-link').removeClass('hidden');
<% end %>